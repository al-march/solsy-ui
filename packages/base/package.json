{
  "name": "@solsy/base",
  "version": "0.0.1",
  "description": "Base components unstyled used by @solsy/daisyui.",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "watch": "tsc --build -w"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.5",
    "clsx": "^1.1.1"
  },
  "peerDependencies": {
    "solid-js": "^1.3.7"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
